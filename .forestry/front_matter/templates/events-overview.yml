---
label: Events Overview
hide_body: true
fields:
- name: content
  type: field_group_list
  fields:
  - name: year
    type: text
    config:
      required: true
    label: Year
  - name: events
    type: textarea
    default: ''
    config:
      required: true
      wysiwyg: true
      schema:
        format: html-blocks
    label: Events
  config:
    min: 
    max: 
    labelField: title
  label: Content
